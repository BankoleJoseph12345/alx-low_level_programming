#include <stdio.h>
/**
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char a;
	char c;

	for (a = 'a'; a <= 'z'; ++a)
	{
		putchar(a);
	}
	for (c = 'A'; c <= 'Z'; ++c)
	{
		purchar(c);
	}
	purchar('\n');
	return (0);
}
